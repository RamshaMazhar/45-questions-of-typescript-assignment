// Define the function to create a shirt
function make_shirt(size, message) {
    console.log("The shirt is size ".concat(size, " and has the message: \"").concat(message, "\"."));
}
// Call the function with example arguments
make_shirt('M', 'Coding is fun!');
make_shirt('L', 'JavaScript is awesome!');
